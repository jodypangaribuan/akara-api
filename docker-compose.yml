services:
  akara-api:
    build: .
    ports:
      - "29999:29999"
    volumes:
      - ./:/app
      - model-cache:/root/.cache
    env_file:
      - .env
    restart: unless-stopped

volumes:
  model-cache:
